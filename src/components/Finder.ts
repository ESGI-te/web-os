import Window from "./Window";

class Finder {
	private window: Window;

	constructor() {
		// this.window = new Window("Finder", 100, 100, 500, 300);
		// const content = document.createElement("div");
		// content.innerText = "Hello from Finder!";
		// this.window.bodyElement.appendChild(content);
	}

	public open() {
		// document.body.appendChild(this.window.windowElement);
	}
}

export default Finder;
